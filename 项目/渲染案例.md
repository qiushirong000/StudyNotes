## 渲染案例
--------------

### 描边效果
- 第一种：two pass顶点外扩描边。一个材质两个pass，第一个pass正常渲染模型本身，剔除背面；第二个pass把模型顶点沿法向方向外扩一点，纯色填充，剔除正面只渲染背面。

- 第二种：在 shader 中开 2 个 Pass 渲染通道，第一个 Pass 通道将待描边物体的屏幕区域像素对应的模板值标记为 1，第二个 Pass 通道将待描边物体的顶点向外膨胀，绘制模板值为非 1 的膨胀区域，即外环区域。

### 消融效果


### 
